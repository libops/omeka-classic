[database]
host = "{{ getenv "DB_HOST" }}"
username = "{{ getenv "DB_USER" }}"
password = "{{ getenv "DB_PASSWORD" }}"
dbname = "{{ getenv "DB_NAME" }}"
prefix = "{{ getenv "OMEKA_CLASSIC_TABLE_PREFIX" }}"
charset = "utf8mb4"
port = "{{ getenv "DB_PORT" }}"

